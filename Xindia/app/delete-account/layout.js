export const metadata = {
  title: 'Delete Your Account — XIndia',
  description:
    'Request deletion of your XIndia account and all associated data. Verify your identity via OTP and submit a secure deletion request. Data is permanently purged after a 15-day cooling-off period.',
  robots: 'index, follow',
};

export default function DeleteAccountLayout({ children }) {
  return children;
}
